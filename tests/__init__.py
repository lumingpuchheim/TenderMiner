"""Tests — see doc/STORAGE.md 6.3.

    python -m unittest discover -t . -s tests     # from the repository root
    python tests/test_storage.py                  # or one module directly

stdlib `unittest` on purpose: pytest is not installed here, and a suite that
needs installing is a suite that does not get run.
"""
